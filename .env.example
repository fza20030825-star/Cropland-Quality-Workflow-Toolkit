# Copy this file to .env if you want local overrides.
# Do not commit .env.

# Optional: absolute path to the parent data directory.
CQE_PROJECT_ROOT=D:\path\to\course-materials

# Optional: set to 1 when running inside an ArcGIS Python environment with arcpy.
CQE_USE_ARCPY=0
